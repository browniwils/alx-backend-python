#!/usr/bin/env python3
"""Basic annotations and types."""

from typing import List


def sum_list(input_list: List[float]) -> float:
    """Add list of float values."""

    return float(sum(input_list))
